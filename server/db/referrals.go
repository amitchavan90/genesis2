// Code generated by SQLBoiler 3.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package db

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// Referral is an object representing the database table.
type Referral struct {
	ID           string      `db:"id" boil:"id" json:"id" toml:"id" yaml:"id"`
	UserID       string      `db:"user_id" boil:"user_id" json:"user_id" toml:"user_id" yaml:"user_id"`
	ReferredByID null.String `db:"referred_by_id" boil:"referred_by_id" json:"referred_by_id,omitempty" toml:"referred_by_id" yaml:"referred_by_id,omitempty"`
	IsRedemmed   bool        `db:"is_redemmed" boil:"is_redemmed" json:"is_redemmed" toml:"is_redemmed" yaml:"is_redemmed"`
	UpdatedAt    time.Time   `db:"updated_at" boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	CreatedAt    time.Time   `db:"created_at" boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *referralR `db:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
	L referralL  `db:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
}

var ReferralColumns = struct {
	ID           string
	UserID       string
	ReferredByID string
	IsRedemmed   string
	UpdatedAt    string
	CreatedAt    string
}{
	ID:           "id",
	UserID:       "user_id",
	ReferredByID: "referred_by_id",
	IsRedemmed:   "is_redemmed",
	UpdatedAt:    "updated_at",
	CreatedAt:    "created_at",
}

// Generated where

var ReferralWhere = struct {
	ID           whereHelperstring
	UserID       whereHelperstring
	ReferredByID whereHelpernull_String
	IsRedemmed   whereHelperbool
	UpdatedAt    whereHelpertime_Time
	CreatedAt    whereHelpertime_Time
}{
	ID:           whereHelperstring{field: "\"referrals\".\"id\""},
	UserID:       whereHelperstring{field: "\"referrals\".\"user_id\""},
	ReferredByID: whereHelpernull_String{field: "\"referrals\".\"referred_by_id\""},
	IsRedemmed:   whereHelperbool{field: "\"referrals\".\"is_redemmed\""},
	UpdatedAt:    whereHelpertime_Time{field: "\"referrals\".\"updated_at\""},
	CreatedAt:    whereHelpertime_Time{field: "\"referrals\".\"created_at\""},
}

// ReferralRels is where relationship names are stored.
var ReferralRels = struct {
	ReferredBy string
	User       string
}{
	ReferredBy: "ReferredBy",
	User:       "User",
}

// referralR is where relationships are stored.
type referralR struct {
	ReferredBy *User
	User       *User
}

// NewStruct creates a new relationship struct
func (*referralR) NewStruct() *referralR {
	return &referralR{}
}

// referralL is where Load methods for each relationship are stored.
type referralL struct{}

var (
	referralAllColumns            = []string{"id", "user_id", "referred_by_id", "is_redemmed", "updated_at", "created_at"}
	referralColumnsWithoutDefault = []string{"user_id", "referred_by_id"}
	referralColumnsWithDefault    = []string{"id", "is_redemmed", "updated_at", "created_at"}
	referralPrimaryKeyColumns     = []string{"id"}
)

type (
	// ReferralSlice is an alias for a slice of pointers to Referral.
	// This should generally be used opposed to []Referral.
	ReferralSlice []*Referral
	// ReferralHook is the signature for custom Referral hook methods
	ReferralHook func(boil.Executor, *Referral) error

	referralQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	referralType                 = reflect.TypeOf(&Referral{})
	referralMapping              = queries.MakeStructMapping(referralType)
	referralPrimaryKeyMapping, _ = queries.BindMapping(referralType, referralMapping, referralPrimaryKeyColumns)
	referralInsertCacheMut       sync.RWMutex
	referralInsertCache          = make(map[string]insertCache)
	referralUpdateCacheMut       sync.RWMutex
	referralUpdateCache          = make(map[string]updateCache)
	referralUpsertCacheMut       sync.RWMutex
	referralUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var referralBeforeInsertHooks []ReferralHook
var referralBeforeUpdateHooks []ReferralHook
var referralBeforeDeleteHooks []ReferralHook
var referralBeforeUpsertHooks []ReferralHook

var referralAfterInsertHooks []ReferralHook
var referralAfterSelectHooks []ReferralHook
var referralAfterUpdateHooks []ReferralHook
var referralAfterDeleteHooks []ReferralHook
var referralAfterUpsertHooks []ReferralHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Referral) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range referralBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Referral) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range referralBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Referral) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range referralBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Referral) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range referralBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Referral) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range referralAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Referral) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range referralAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Referral) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range referralAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Referral) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range referralAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Referral) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range referralAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddReferralHook registers your hook function for all future operations.
func AddReferralHook(hookPoint boil.HookPoint, referralHook ReferralHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		referralBeforeInsertHooks = append(referralBeforeInsertHooks, referralHook)
	case boil.BeforeUpdateHook:
		referralBeforeUpdateHooks = append(referralBeforeUpdateHooks, referralHook)
	case boil.BeforeDeleteHook:
		referralBeforeDeleteHooks = append(referralBeforeDeleteHooks, referralHook)
	case boil.BeforeUpsertHook:
		referralBeforeUpsertHooks = append(referralBeforeUpsertHooks, referralHook)
	case boil.AfterInsertHook:
		referralAfterInsertHooks = append(referralAfterInsertHooks, referralHook)
	case boil.AfterSelectHook:
		referralAfterSelectHooks = append(referralAfterSelectHooks, referralHook)
	case boil.AfterUpdateHook:
		referralAfterUpdateHooks = append(referralAfterUpdateHooks, referralHook)
	case boil.AfterDeleteHook:
		referralAfterDeleteHooks = append(referralAfterDeleteHooks, referralHook)
	case boil.AfterUpsertHook:
		referralAfterUpsertHooks = append(referralAfterUpsertHooks, referralHook)
	}
}

// One returns a single referral record from the query.
func (q referralQuery) One(exec boil.Executor) (*Referral, error) {
	o := &Referral{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "db: failed to execute a one query for referrals")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Referral records from the query.
func (q referralQuery) All(exec boil.Executor) (ReferralSlice, error) {
	var o []*Referral

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "db: failed to assign all query results to Referral slice")
	}

	if len(referralAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Referral records in the query.
func (q referralQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to count referrals rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q referralQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "db: failed to check if referrals exists")
	}

	return count > 0, nil
}

// ReferredBy pointed to by the foreign key.
func (o *Referral) ReferredBy(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ReferredByID),
	}

	queryMods = append(queryMods, mods...)

	query := Users(queryMods...)
	queries.SetFrom(query.Query, "\"users\"")

	return query
}

// User pointed to by the foreign key.
func (o *Referral) User(mods ...qm.QueryMod) userQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UserID),
	}

	queryMods = append(queryMods, mods...)

	query := Users(queryMods...)
	queries.SetFrom(query.Query, "\"users\"")

	return query
}

// LoadReferredBy allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (referralL) LoadReferredBy(e boil.Executor, singular bool, maybeReferral interface{}, mods queries.Applicator) error {
	var slice []*Referral
	var object *Referral

	if singular {
		object = maybeReferral.(*Referral)
	} else {
		slice = *maybeReferral.(*[]*Referral)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &referralR{}
		}
		if !queries.IsNil(object.ReferredByID) {
			args = append(args, object.ReferredByID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &referralR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.ReferredByID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.ReferredByID) {
				args = append(args, obj.ReferredByID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`users`), qm.WhereIn(`users.id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(referralAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.ReferredBy = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.ReferredByReferrals = append(foreign.R.ReferredByReferrals, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.ReferredByID, foreign.ID) {
				local.R.ReferredBy = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.ReferredByReferrals = append(foreign.R.ReferredByReferrals, local)
				break
			}
		}
	}

	return nil
}

// LoadUser allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (referralL) LoadUser(e boil.Executor, singular bool, maybeReferral interface{}, mods queries.Applicator) error {
	var slice []*Referral
	var object *Referral

	if singular {
		object = maybeReferral.(*Referral)
	} else {
		slice = *maybeReferral.(*[]*Referral)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &referralR{}
		}
		args = append(args, object.UserID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &referralR{}
			}

			for _, a := range args {
				if a == obj.UserID {
					continue Outer
				}
			}

			args = append(args, obj.UserID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`users`), qm.WhereIn(`users.id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load User")
	}

	var resultSlice []*User
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice User")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for users")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for users")
	}

	if len(referralAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.User = foreign
		if foreign.R == nil {
			foreign.R = &userR{}
		}
		foreign.R.Referrals = append(foreign.R.Referrals, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.UserID == foreign.ID {
				local.R.User = foreign
				if foreign.R == nil {
					foreign.R = &userR{}
				}
				foreign.R.Referrals = append(foreign.R.Referrals, local)
				break
			}
		}
	}

	return nil
}

// SetReferredBy of the referral to the related item.
// Sets o.R.ReferredBy to related.
// Adds o to related.R.ReferredByReferrals.
func (o *Referral) SetReferredBy(exec boil.Executor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"referrals\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"referred_by_id"}),
		strmangle.WhereClause("\"", "\"", 2, referralPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.ReferredByID, related.ID)
	if o.R == nil {
		o.R = &referralR{
			ReferredBy: related,
		}
	} else {
		o.R.ReferredBy = related
	}

	if related.R == nil {
		related.R = &userR{
			ReferredByReferrals: ReferralSlice{o},
		}
	} else {
		related.R.ReferredByReferrals = append(related.R.ReferredByReferrals, o)
	}

	return nil
}

// RemoveReferredBy relationship.
// Sets o.R.ReferredBy to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *Referral) RemoveReferredBy(exec boil.Executor, related *User) error {
	var err error

	queries.SetScanner(&o.ReferredByID, nil)
	if _, err = o.Update(exec, boil.Whitelist("referred_by_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.ReferredBy = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.ReferredByReferrals {
		if queries.Equal(o.ReferredByID, ri.ReferredByID) {
			continue
		}

		ln := len(related.R.ReferredByReferrals)
		if ln > 1 && i < ln-1 {
			related.R.ReferredByReferrals[i] = related.R.ReferredByReferrals[ln-1]
		}
		related.R.ReferredByReferrals = related.R.ReferredByReferrals[:ln-1]
		break
	}
	return nil
}

// SetUser of the referral to the related item.
// Sets o.R.User to related.
// Adds o to related.R.Referrals.
func (o *Referral) SetUser(exec boil.Executor, insert bool, related *User) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"referrals\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"user_id"}),
		strmangle.WhereClause("\"", "\"", 2, referralPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.UserID = related.ID
	if o.R == nil {
		o.R = &referralR{
			User: related,
		}
	} else {
		o.R.User = related
	}

	if related.R == nil {
		related.R = &userR{
			Referrals: ReferralSlice{o},
		}
	} else {
		related.R.Referrals = append(related.R.Referrals, o)
	}

	return nil
}

// Referrals retrieves all the records using an executor.
func Referrals(mods ...qm.QueryMod) referralQuery {
	mods = append(mods, qm.From("\"referrals\""))
	return referralQuery{NewQuery(mods...)}
}

// FindReferral retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindReferral(exec boil.Executor, iD string, selectCols ...string) (*Referral, error) {
	referralObj := &Referral{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"referrals\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, referralObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "db: unable to select from referrals")
	}

	return referralObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Referral) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("db: no referrals provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.UpdatedAt.IsZero() {
		o.UpdatedAt = currTime
	}
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(referralColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	referralInsertCacheMut.RLock()
	cache, cached := referralInsertCache[key]
	referralInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			referralAllColumns,
			referralColumnsWithDefault,
			referralColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(referralType, referralMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(referralType, referralMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"referrals\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"referrals\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "db: unable to insert into referrals")
	}

	if !cached {
		referralInsertCacheMut.Lock()
		referralInsertCache[key] = cache
		referralInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// Update uses an executor to update the Referral.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Referral) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	referralUpdateCacheMut.RLock()
	cache, cached := referralUpdateCache[key]
	referralUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			referralAllColumns,
			referralPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("db: unable to update referrals, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"referrals\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, referralPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(referralType, referralMapping, append(wl, referralPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to update referrals row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to get rows affected by update for referrals")
	}

	if !cached {
		referralUpdateCacheMut.Lock()
		referralUpdateCache[key] = cache
		referralUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q referralQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to update all for referrals")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to retrieve rows affected for referrals")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o ReferralSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("db: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), referralPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"referrals\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, referralPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to update all in referral slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to retrieve rows affected all in update all referral")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Referral) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("db: no referrals provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(referralColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	referralUpsertCacheMut.RLock()
	cache, cached := referralUpsertCache[key]
	referralUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			referralAllColumns,
			referralColumnsWithDefault,
			referralColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			referralAllColumns,
			referralPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("db: unable to upsert referrals, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(referralPrimaryKeyColumns))
			copy(conflict, referralPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"referrals\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(referralType, referralMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(referralType, referralMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "db: unable to upsert referrals")
	}

	if !cached {
		referralUpsertCacheMut.Lock()
		referralUpsertCache[key] = cache
		referralUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// Delete deletes a single Referral record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Referral) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("db: no Referral provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), referralPrimaryKeyMapping)
	sql := "DELETE FROM \"referrals\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to delete from referrals")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to get rows affected by delete for referrals")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q referralQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("db: no referralQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to delete all from referrals")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to get rows affected by deleteall for referrals")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o ReferralSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(referralBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), referralPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"referrals\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, referralPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to delete all from referral slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to get rows affected by deleteall for referrals")
	}

	if len(referralAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Referral) Reload(exec boil.Executor) error {
	ret, err := FindReferral(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *ReferralSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := ReferralSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), referralPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"referrals\".* FROM \"referrals\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, referralPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "db: unable to reload all in ReferralSlice")
	}

	*o = slice

	return nil
}

// ReferralExists checks if the Referral row exists.
func ReferralExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"referrals\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "db: unable to check if referrals exists")
	}

	return exists, nil
}
