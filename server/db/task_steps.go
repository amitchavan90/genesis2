// Code generated by SQLBoiler 3.7.1 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package db

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null"
	"github.com/volatiletech/sqlboiler/boil"
	"github.com/volatiletech/sqlboiler/queries"
	"github.com/volatiletech/sqlboiler/queries/qm"
	"github.com/volatiletech/sqlboiler/queries/qmhelper"
	"github.com/volatiletech/sqlboiler/strmangle"
)

// TaskStep is an object representing the database table.
type TaskStep struct {
	ID          string      `db:"id" boil:"id" json:"id" toml:"id" yaml:"id"`
	TaskID      null.String `db:"task_id" boil:"task_id" json:"task_id,omitempty" toml:"task_id" yaml:"task_id,omitempty"`
	Name        string      `db:"name" boil:"name" json:"name" toml:"name" yaml:"name"`
	Description string      `db:"description" boil:"description" json:"description" toml:"description" yaml:"description"`
	UpdatedAt   time.Time   `db:"updated_at" boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	CreatedAt   time.Time   `db:"created_at" boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *taskStepR `db:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
	L taskStepL  `db:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TaskStepColumns = struct {
	ID          string
	TaskID      string
	Name        string
	Description string
	UpdatedAt   string
	CreatedAt   string
}{
	ID:          "id",
	TaskID:      "task_id",
	Name:        "name",
	Description: "description",
	UpdatedAt:   "updated_at",
	CreatedAt:   "created_at",
}

// Generated where

var TaskStepWhere = struct {
	ID          whereHelperstring
	TaskID      whereHelpernull_String
	Name        whereHelperstring
	Description whereHelperstring
	UpdatedAt   whereHelpertime_Time
	CreatedAt   whereHelpertime_Time
}{
	ID:          whereHelperstring{field: "\"task_steps\".\"id\""},
	TaskID:      whereHelpernull_String{field: "\"task_steps\".\"task_id\""},
	Name:        whereHelperstring{field: "\"task_steps\".\"name\""},
	Description: whereHelperstring{field: "\"task_steps\".\"description\""},
	UpdatedAt:   whereHelpertime_Time{field: "\"task_steps\".\"updated_at\""},
	CreatedAt:   whereHelpertime_Time{field: "\"task_steps\".\"created_at\""},
}

// TaskStepRels is where relationship names are stored.
var TaskStepRels = struct {
	Task string
}{
	Task: "Task",
}

// taskStepR is where relationships are stored.
type taskStepR struct {
	Task *Task
}

// NewStruct creates a new relationship struct
func (*taskStepR) NewStruct() *taskStepR {
	return &taskStepR{}
}

// taskStepL is where Load methods for each relationship are stored.
type taskStepL struct{}

var (
	taskStepAllColumns            = []string{"id", "task_id", "name", "description", "updated_at", "created_at"}
	taskStepColumnsWithoutDefault = []string{"task_id", "name", "description"}
	taskStepColumnsWithDefault    = []string{"id", "updated_at", "created_at"}
	taskStepPrimaryKeyColumns     = []string{"id"}
)

type (
	// TaskStepSlice is an alias for a slice of pointers to TaskStep.
	// This should generally be used opposed to []TaskStep.
	TaskStepSlice []*TaskStep
	// TaskStepHook is the signature for custom TaskStep hook methods
	TaskStepHook func(boil.Executor, *TaskStep) error

	taskStepQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	taskStepType                 = reflect.TypeOf(&TaskStep{})
	taskStepMapping              = queries.MakeStructMapping(taskStepType)
	taskStepPrimaryKeyMapping, _ = queries.BindMapping(taskStepType, taskStepMapping, taskStepPrimaryKeyColumns)
	taskStepInsertCacheMut       sync.RWMutex
	taskStepInsertCache          = make(map[string]insertCache)
	taskStepUpdateCacheMut       sync.RWMutex
	taskStepUpdateCache          = make(map[string]updateCache)
	taskStepUpsertCacheMut       sync.RWMutex
	taskStepUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var taskStepBeforeInsertHooks []TaskStepHook
var taskStepBeforeUpdateHooks []TaskStepHook
var taskStepBeforeDeleteHooks []TaskStepHook
var taskStepBeforeUpsertHooks []TaskStepHook

var taskStepAfterInsertHooks []TaskStepHook
var taskStepAfterSelectHooks []TaskStepHook
var taskStepAfterUpdateHooks []TaskStepHook
var taskStepAfterDeleteHooks []TaskStepHook
var taskStepAfterUpsertHooks []TaskStepHook

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TaskStep) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range taskStepBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TaskStep) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range taskStepBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TaskStep) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range taskStepBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TaskStep) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range taskStepBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TaskStep) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range taskStepAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TaskStep) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range taskStepAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TaskStep) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range taskStepAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TaskStep) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range taskStepAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TaskStep) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range taskStepAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTaskStepHook registers your hook function for all future operations.
func AddTaskStepHook(hookPoint boil.HookPoint, taskStepHook TaskStepHook) {
	switch hookPoint {
	case boil.BeforeInsertHook:
		taskStepBeforeInsertHooks = append(taskStepBeforeInsertHooks, taskStepHook)
	case boil.BeforeUpdateHook:
		taskStepBeforeUpdateHooks = append(taskStepBeforeUpdateHooks, taskStepHook)
	case boil.BeforeDeleteHook:
		taskStepBeforeDeleteHooks = append(taskStepBeforeDeleteHooks, taskStepHook)
	case boil.BeforeUpsertHook:
		taskStepBeforeUpsertHooks = append(taskStepBeforeUpsertHooks, taskStepHook)
	case boil.AfterInsertHook:
		taskStepAfterInsertHooks = append(taskStepAfterInsertHooks, taskStepHook)
	case boil.AfterSelectHook:
		taskStepAfterSelectHooks = append(taskStepAfterSelectHooks, taskStepHook)
	case boil.AfterUpdateHook:
		taskStepAfterUpdateHooks = append(taskStepAfterUpdateHooks, taskStepHook)
	case boil.AfterDeleteHook:
		taskStepAfterDeleteHooks = append(taskStepAfterDeleteHooks, taskStepHook)
	case boil.AfterUpsertHook:
		taskStepAfterUpsertHooks = append(taskStepAfterUpsertHooks, taskStepHook)
	}
}

// One returns a single taskStep record from the query.
func (q taskStepQuery) One(exec boil.Executor) (*TaskStep, error) {
	o := &TaskStep{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "db: failed to execute a one query for task_steps")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TaskStep records from the query.
func (q taskStepQuery) All(exec boil.Executor) (TaskStepSlice, error) {
	var o []*TaskStep

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "db: failed to assign all query results to TaskStep slice")
	}

	if len(taskStepAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TaskStep records in the query.
func (q taskStepQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to count task_steps rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q taskStepQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "db: failed to check if task_steps exists")
	}

	return count > 0, nil
}

// Task pointed to by the foreign key.
func (o *TaskStep) Task(mods ...qm.QueryMod) taskQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.TaskID),
	}

	queryMods = append(queryMods, mods...)

	query := Tasks(queryMods...)
	queries.SetFrom(query.Query, "\"tasks\"")

	return query
}

// LoadTask allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (taskStepL) LoadTask(e boil.Executor, singular bool, maybeTaskStep interface{}, mods queries.Applicator) error {
	var slice []*TaskStep
	var object *TaskStep

	if singular {
		object = maybeTaskStep.(*TaskStep)
	} else {
		slice = *maybeTaskStep.(*[]*TaskStep)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &taskStepR{}
		}
		if !queries.IsNil(object.TaskID) {
			args = append(args, object.TaskID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &taskStepR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.TaskID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.TaskID) {
				args = append(args, obj.TaskID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(qm.From(`tasks`), qm.WhereIn(`tasks.id in ?`, args...))
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Task")
	}

	var resultSlice []*Task
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Task")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for tasks")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for tasks")
	}

	if len(taskStepAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Task = foreign
		if foreign.R == nil {
			foreign.R = &taskR{}
		}
		foreign.R.TaskSteps = append(foreign.R.TaskSteps, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.TaskID, foreign.ID) {
				local.R.Task = foreign
				if foreign.R == nil {
					foreign.R = &taskR{}
				}
				foreign.R.TaskSteps = append(foreign.R.TaskSteps, local)
				break
			}
		}
	}

	return nil
}

// SetTask of the taskStep to the related item.
// Sets o.R.Task to related.
// Adds o to related.R.TaskSteps.
func (o *TaskStep) SetTask(exec boil.Executor, insert bool, related *Task) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"task_steps\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"task_id"}),
		strmangle.WhereClause("\"", "\"", 2, taskStepPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.TaskID, related.ID)
	if o.R == nil {
		o.R = &taskStepR{
			Task: related,
		}
	} else {
		o.R.Task = related
	}

	if related.R == nil {
		related.R = &taskR{
			TaskSteps: TaskStepSlice{o},
		}
	} else {
		related.R.TaskSteps = append(related.R.TaskSteps, o)
	}

	return nil
}

// RemoveTask relationship.
// Sets o.R.Task to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *TaskStep) RemoveTask(exec boil.Executor, related *Task) error {
	var err error

	queries.SetScanner(&o.TaskID, nil)
	if _, err = o.Update(exec, boil.Whitelist("task_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Task = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.TaskSteps {
		if queries.Equal(o.TaskID, ri.TaskID) {
			continue
		}

		ln := len(related.R.TaskSteps)
		if ln > 1 && i < ln-1 {
			related.R.TaskSteps[i] = related.R.TaskSteps[ln-1]
		}
		related.R.TaskSteps = related.R.TaskSteps[:ln-1]
		break
	}
	return nil
}

// TaskSteps retrieves all the records using an executor.
func TaskSteps(mods ...qm.QueryMod) taskStepQuery {
	mods = append(mods, qm.From("\"task_steps\""))
	return taskStepQuery{NewQuery(mods...)}
}

// FindTaskStep retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTaskStep(exec boil.Executor, iD string, selectCols ...string) (*TaskStep, error) {
	taskStepObj := &TaskStep{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"task_steps\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, taskStepObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "db: unable to select from task_steps")
	}

	return taskStepObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TaskStep) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("db: no task_steps provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.UpdatedAt.IsZero() {
		o.UpdatedAt = currTime
	}
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(taskStepColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	taskStepInsertCacheMut.RLock()
	cache, cached := taskStepInsertCache[key]
	taskStepInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			taskStepAllColumns,
			taskStepColumnsWithDefault,
			taskStepColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(taskStepType, taskStepMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(taskStepType, taskStepMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"task_steps\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"task_steps\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "db: unable to insert into task_steps")
	}

	if !cached {
		taskStepInsertCacheMut.Lock()
		taskStepInsertCache[key] = cache
		taskStepInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// Update uses an executor to update the TaskStep.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TaskStep) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	taskStepUpdateCacheMut.RLock()
	cache, cached := taskStepUpdateCache[key]
	taskStepUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			taskStepAllColumns,
			taskStepPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("db: unable to update task_steps, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"task_steps\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, taskStepPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(taskStepType, taskStepMapping, append(wl, taskStepPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to update task_steps row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to get rows affected by update for task_steps")
	}

	if !cached {
		taskStepUpdateCacheMut.Lock()
		taskStepUpdateCache[key] = cache
		taskStepUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q taskStepQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to update all for task_steps")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to retrieve rows affected for task_steps")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TaskStepSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("db: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), taskStepPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"task_steps\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, taskStepPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to update all in taskStep slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to retrieve rows affected all in update all taskStep")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TaskStep) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("db: no task_steps provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(taskStepColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	taskStepUpsertCacheMut.RLock()
	cache, cached := taskStepUpsertCache[key]
	taskStepUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			taskStepAllColumns,
			taskStepColumnsWithDefault,
			taskStepColumnsWithoutDefault,
			nzDefaults,
		)
		update := updateColumns.UpdateColumnSet(
			taskStepAllColumns,
			taskStepPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("db: unable to upsert task_steps, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(taskStepPrimaryKeyColumns))
			copy(conflict, taskStepPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"task_steps\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(taskStepType, taskStepMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(taskStepType, taskStepMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "db: unable to upsert task_steps")
	}

	if !cached {
		taskStepUpsertCacheMut.Lock()
		taskStepUpsertCache[key] = cache
		taskStepUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// Delete deletes a single TaskStep record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TaskStep) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("db: no TaskStep provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), taskStepPrimaryKeyMapping)
	sql := "DELETE FROM \"task_steps\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to delete from task_steps")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to get rows affected by delete for task_steps")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q taskStepQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("db: no taskStepQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to delete all from task_steps")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to get rows affected by deleteall for task_steps")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TaskStepSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(taskStepBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), taskStepPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"task_steps\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, taskStepPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "db: unable to delete all from taskStep slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "db: failed to get rows affected by deleteall for task_steps")
	}

	if len(taskStepAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TaskStep) Reload(exec boil.Executor) error {
	ret, err := FindTaskStep(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TaskStepSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TaskStepSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), taskStepPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"task_steps\".* FROM \"task_steps\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, taskStepPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "db: unable to reload all in TaskStepSlice")
	}

	*o = slice

	return nil
}

// TaskStepExists checks if the TaskStep row exists.
func TaskStepExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"task_steps\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "db: unable to check if task_steps exists")
	}

	return exists, nil
}
